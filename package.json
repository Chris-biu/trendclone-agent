{
  "name": "trendclone-agent",
  "version": "0.2.0",
  "private": true,
  "description": "AI short-video trend cloning and originality-safe creative package generator.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check server.js && node --check server/agent-core.js"
  },
  "engines": {
    "node": ">=18"
  }
}
